# 팀원 시작 안내

> 컴퓨터를 잘 몰라도 됩니다. **위에서부터 순서대로** 따라 하시면 됩니다.
> 전부 처음이면 20~30분 걸리고, **한 번만 하면 됩니다.**
>
> 막히면 그 화면을 캡처해서 물어보십시오. 대부분 아래 `막혔을 때` 에 있습니다.

---

## 0. 이 프로그램이 뭔가요

엑셀 평가지를 넣으면 **사람마다 개인 피드백 리포트를 만들어 주는 프로그램**입니다.
웹사이트처럼 생겼지만 인터넷에 올라가 있는 게 아니라, **내 컴퓨터에서 돌아갑니다.**
그래서 내 컴퓨터에 한 번 설치해야 합니다.

---

## 1. 터미널 여는 법

터미널은 **글자로 명령을 내리는 검은 창**입니다. 설치할 때만 잠깐 씁니다.

### 여는 방법 (셋 중 아무거나)

**① 제일 쉬운 방법**

키보드에서 **`윈도우키` + `R`** 을 같이 누릅니다 → 작은 창이 뜨면
`cmd` 라고 치고 **엔터**.

```
윈도우키 + R  →  cmd  →  엔터
```

**② 원하는 폴더에서 바로 열기 (이게 제일 편합니다)**

폴더를 하나 연 다음, **위쪽 주소가 적힌 칸을 클릭**해서 글자를 지우고
`cmd` 라고 치고 **엔터**.

```
[ C:\Users\내이름\Desktop ]   ← 이 칸을 클릭
[ cmd                    ]   ← 이렇게 바꾸고 엔터
```

이렇게 열면 **그 폴더에서** 터미널이 열려서 편합니다.

**③ 시작 버튼에서**

시작 버튼 누르고 `cmd` 라고 치면 `명령 프롬프트` 가 나옵니다. 클릭.

### 터미널 쓰는 법

- 명령을 치고 **엔터**를 눌러야 실행됩니다.
- 붙여넣기는 **`Ctrl` + `V`** (안 되면 **마우스 우클릭**)
- 글자가 안 보여도 정상입니다 (비밀번호 칠 때)
- 창을 닫으려면 `exit` 치고 엔터, 또는 그냥 X 버튼

---

## 2. Git 설치하기

Git 은 **파일을 최신으로 맞춰 주는 프로그램**입니다. 이게 있어야 나중에
수정된 내용을 한 줄로 받아올 수 있습니다.

1. <https://git-scm.com/download/win> 접속
2. 자동으로 다운로드가 시작됩니다 (안 되면 `Click here to download` 클릭)
3. 받은 파일 실행 → **계속 `Next` 만 누르고** 마지막에 `Install`
4. 다 되면 `Finish`

### 설치됐는지 확인

터미널을 **새로 열고** 아래를 치고 엔터.

```
git --version
```

`git version 2.xx.x` 같은 게 나오면 성공입니다.
`'git'은(는) ... 인식되지 않습니다` 가 나오면 → 터미널을 **닫았다가 다시 여십시오.**
그래도 안 되면 설치가 안 된 것이니 2번을 다시 하십시오.

---

## 3. Python 설치하기

프로그램이 파이썬으로 만들어져 있어서 필요합니다.

### 먼저 이미 있는지 확인

터미널에 아래를 치고 엔터.

```
py --version
```

`Python 3.10.x` 이상이 나오면 **3번은 건너뛰십시오.**

### 없다면 설치

1. <https://www.python.org/downloads/> 접속
2. 노란 버튼 `Download Python 3.x.x` 클릭
3. 받은 파일 실행

> ⚠️ **첫 화면에서 맨 아래 `Add python.exe to PATH` 를 반드시 체크하십시오.**
> 이걸 놓치면 나중에 프로그램이 파이썬을 못 찾습니다. 제일 많이 하는 실수입니다.

4. `Install Now` 클릭 → 끝나면 `Close`
5. 터미널을 **닫았다가 다시 열고** `py --version` 으로 확인

---

## 4. 프로그램 받기

### 4-1. 폴더 만들기

`문서` 나 `바탕 화면` 에 폴더를 하나 만드십시오. 이름은 아무거나 (예: `과제`).

### 4-2. 그 폴더에서 터미널 열기

만든 폴더를 열고, **주소 칸에 `cmd` 치고 엔터** (1장 ② 방법).

### 4-3. 받기

아래를 그대로 붙여넣고 엔터.

```
git clone https://github.com/lsm040611/AI-report.git
```

글자가 주르륵 지나가고 멈추면 끝입니다. 폴더 안에 `AI-report` 라는
폴더가 생겼을 겁니다.

> **`Download ZIP` 으로 받으면 안 되나요?**
> 실행은 되지만 **나중에 수정본을 못 받습니다.** 매번 다시 받아야 합니다.
> 위 방법으로 받으시면 나중에 명령 한 줄이면 최신이 됩니다.

---

## 5. 필요한 것 설치하기

터미널에서 방금 받은 폴더로 들어갑니다.

```
cd AI-report
```

그다음 아래를 붙여넣고 엔터. (1~2분 걸립니다)

```
py -3.10 -m pip install -r requirements.txt
```

> `py -3.10` 에서 오류가 나면 `py` 만 써 보십시오.
> ```
> py -m pip install -r requirements.txt
> ```

글자가 한참 지나가고 `Successfully installed ...` 가 나오면 성공입니다.

---

## 6. 실행하기

**여기서부터는 터미널을 안 써도 됩니다.**

`AI-report` 폴더를 열고 **`start.bat` 을 더블클릭**하십시오.

- 검은 창이 하나 뜨고 (이건 켜 두셔야 합니다)
- 잠시 뒤 브라우저가 자동으로 열립니다

안 열리면 브라우저 주소창에 직접 치십시오.

```
http://127.0.0.1:8000
```

### 써 보기

1. 화면에서 **파일 선택** → 엑셀 평가지 고르기 (여러 개 한 번에 가능)
2. **만들기** 누르기
3. 사람마다 **리포트 열기 ↗** 링크가 나옵니다
4. 리포트를 열고 `Ctrl` + `P` → `대상: PDF로 저장` 하면 PDF가 됩니다

### 끄기

검은 창을 닫으면 꺼집니다. 뭔가 남은 것 같으면 `stop.bat` 더블클릭.

---

## 7. 나중에 최신으로 맞추기

수정된 내용이 생기면 이 한 줄이면 됩니다.

1. `AI-report` 폴더를 엽니다
2. 주소 칸에 `cmd` 치고 엔터
3. 아래를 붙여넣고 엔터

```
git pull
```

| 나오는 말 | 뜻 |
|---|---|
| `Already up to date.` | 이미 최신입니다. 할 것 없음 |
| `Fast-forward` 와 파일 목록 | 잘 받아졌습니다 |
| `Your local changes ...` | 본인이 파일을 고쳤습니다 → 아래 참고 |

파일을 고쳐서 막혔다면 고친 것을 잠시 치워 두고 받으십시오.

```
git stash
git pull
```

그냥 `AI-report` 폴더를 통째로 지우고 4장부터 다시 해도 됩니다.

---

## 8. 메일 발송은 안 하셔도 됩니다

**이 설정을 안 해도 프로그램은 전부 돌아갑니다.** 리포트도 다 만들어집니다.

메일 버튼을 누르면 "누구에게 무엇이 갈지" 만 보여 주고 **실제로는 안 나갑니다.**
발표나 확인용으로는 이걸로 충분합니다.

직접 보내 보고 싶으시면 `README.md` 의 **`리포트를 본인 메일로 보내기`** 를
보십시오. 본인 지메일 계정으로 10분이면 됩니다. 남의 계정은 쓸 수 없습니다.

---

## 9. 막혔을 때

| 화면에 나오는 말 | 뜻 · 해결 |
|---|---|
| `'git'은(는) ... 인식되지 않습니다` | Git 미설치. 터미널을 새로 열어 보고, 그래도 안 되면 2장 다시 |
| `'py'은(는) ... 인식되지 않습니다` | Python 미설치이거나 `Add python.exe to PATH` 를 안 함. 3장 다시 (설치할 때 체크 꼭) |
| `ModuleNotFoundError: No module named 'fastapi'` | 5장을 안 했습니다. 5장 실행 |
| `start.bat` 을 눌러도 창이 깜빡하고 사라짐 | 5장이 안 된 것입니다. 터미널에서 `py -3.10 -m uvicorn main:app --port 8000` 를 쳐 보면 이유가 보입니다 |
| 브라우저에 `연결할 수 없음` | 검은 창이 꺼져 있습니다. `start.bat` 다시 |
| `Address already in use` / `10048` | 이미 켜져 있습니다. `stop.bat` 누르고 다시 |
| 업로드했더니 `xlsx 파일만 지원합니다` | `.xls`, `.csv` 는 안 됩니다. 엑셀에서 `.xlsx` 로 저장하십시오 |
| 리포트에 `목(mock)` 이라고 표기됨 | 정상입니다. AI 문장 대신 단순 문장이 들어간 것입니다 |

### 그래도 안 되면

이 세 가지를 같이 알려 주십시오. 대부분 바로 원인이 나옵니다.

1. **어느 장, 몇 번**에서 막혔는지
2. **화면 캡처** (검은 창 전체가 보이게)
3. `py --version` 과 `git --version` 결과
