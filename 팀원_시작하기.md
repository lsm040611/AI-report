# AI 리포트 엔진 시작 안내

안녕하세요. AI 리포트 엔진 받는 방법입니다. 순서대로 따라와 주세요.
컴퓨터 잘 몰라도 됩니다. 처음이면 20분쯤 걸리고, **한 번만 하면 됩니다.**

> ⚠️ **먼저 — 전에 ZIP 파일로 받으신 분은 그 폴더를 지워주세요.**
> ZIP으로 받으면 나중에 수정된 내용을 못 받습니다. 아래 방법으로 다시
> 받으시면 그다음부터는 명령어 한 줄로 최신이 됩니다.

---

## 1단계. 터미널 여는 법

터미널은 글자로 명령을 내리는 검은 창입니다. 설치할 때만 잠깐 씁니다.

`윈도우키` + `R` 을 같이 누르고 → `cmd` 치고 → 엔터

붙여넣기는 `Ctrl` + `V` (안 되면 마우스 우클릭)

## 2단계. Git 설치

<https://git-scm.com/download/win> 들어가서 다운로드 → 실행 →
**계속 Next만 누르고** 마지막에 Install

설치 후 터미널을 **새로 열고** 확인:

```
git --version
```

`git version 2.xx.x` 나오면 성공입니다.

## 3단계. Python 설치

터미널에 `py --version` 쳐보세요. `Python 3.10` 이상 나오면 3단계는 건너뛰세요.

없으면 <https://www.python.org/downloads/> 에서 노란 버튼으로 받아서 실행.

> ⚠️ **첫 화면 맨 아래 `Add python.exe to PATH` 반드시 체크하세요.**
> 이거 놓치면 나중에 안 됩니다. 제일 많이 하는 실수예요.

체크하고 → Install Now → 끝나면 터미널 **새로 열어서** `py --version` 확인

## 4단계. 프로그램 받기

바탕화면에 폴더 하나 만드세요 (이름 아무거나).

그 폴더를 열고 → **위쪽 주소 적힌 칸을 클릭** → 글자 지우고 `cmd` 치고 엔터
(이렇게 하면 그 폴더에서 터미널이 열립니다)

그다음 아래 붙여넣고 엔터:

```
git clone https://github.com/lsm040611/AI-report.git
```

글자 주르륵 지나가고 멈추면 끝. `AI-report` 폴더가 생겼을 거예요.

## 5단계. 필요한 것 설치

같은 터미널에서:

```
cd AI-report
py -3.10 -m pip install -r requirements.txt
```

1~2분 걸립니다. `Successfully installed` 나오면 성공.

(`py -3.10` 에서 오류나면 `py -m pip install -r requirements.txt` 로 해보세요)

## 6단계. 실행 — 여기서부터 터미널 안 씁니다

`AI-report` 폴더 열고 **`start.bat` 더블클릭**

검은 창 하나 뜨고(이건 켜 두세요) 브라우저가 자동으로 열립니다.
안 열리면 주소창에 `http://127.0.0.1:8000`

엑셀 평가지 올리고 → 만들기 → 사람마다 리포트 링크가 나옵니다.
리포트 열고 `Ctrl+P` → 대상을 `PDF로 저장` 하면 PDF도 됩니다.

끄실 때는 검은 창 닫으시면 됩니다.

---

## 📌 앞으로 업데이트되면 (중요)

다시 받을 필요 없습니다. `AI-report` 폴더 열고 → 주소칸에 `cmd` → 아래 한 줄:

```
git pull
```

전체를 다시 받는 게 아니라 바뀐 부분만 몇 초 만에 옵니다.
(`Already up to date.` 나오면 이미 최신이라는 뜻입니다)

---

## 📧 메일 발송 설정 (안 하셔도 됩니다)

**이거 안 해도 프로그램은 전부 돌아갑니다.** 리포트도 다 만들어집니다.
메일 버튼을 누르면 "누구에게 무엇이 갈지"까지 보여주고 실제로만 안 나갑니다.
확인용으로는 이걸로 충분해요.

직접 보내보고 싶으신 분만 아래 하시면 됩니다. **본인 지메일**로 하세요.
10분 걸립니다.

### ① 2단계 인증 켜기

<https://myaccount.google.com/security> 접속
→ `Google에 로그인하는 방법` → `2단계 인증` → 시작하기
→ 휴대폰 번호 입력 → 문자로 온 6자리 입력 → 사용 설정

### ② 앱 비밀번호 받기

<https://myaccount.google.com/apppasswords> 접속
→ 이름 칸에 아무거나 (예: HR리포트) → 만들기
→ **16자리**가 뜹니다 (`abcd efgh ijkl mnop` 이런 식)
→ 복사해두세요. **이 화면은 한 번만 보입니다.**

> 앱 비밀번호는 메일 프로그램 전용 열쇠라서, 이걸로는 구글 계정에 로그인
> 못 합니다. 지메일 화면도 못 열어요. 메일 보내는 것만 됩니다.
> 언제든 위 주소에서 삭제하면 무효가 됩니다.

### ③ 설정 파일 만들기

`AI-report` 폴더(`start.bat` 있는 그 폴더)에서 메모장을 열고 아래 5줄 붙여넣기:

```
HR_SMTP_HOST=smtp.gmail.com
HR_SMTP_PORT=587
HR_SMTP_USER=본인아이디@gmail.com
HR_SMTP_PASS=여기에16자리
HR_MAIL_ENABLED=1
```

- `HR_SMTP_USER` 는 본인 지메일 주소를 **@gmail.com까지** 전부
- `HR_SMTP_PASS` 는 ②에서 받은 16자리 (띄어쓰기 있어도 됩니다)

저장할 때 **파일 이름을 `mail.env`** 로, **파일 형식을 `모든 파일`** 로 바꾸세요.
그냥 저장하면 `mail.env.txt` 가 돼서 프로그램이 못 찾습니다.
**여기서 제일 많이 막힙니다.**

### ④ 확인

`start.bat` 다시 실행 → 리포트 만들고 → `본인 메일로 보내기` 클릭
`4/4명에게 보냈습니다` 나오면 성공입니다.

---

## 막힐 때

| 나오는 말 | 뜻 |
|---|---|
| `'git'은(는) 인식되지 않습니다` | 터미널 새로 열어보세요. 그래도 안 되면 2단계 다시 |
| `'py'은(는) 인식되지 않습니다` | Python 설치 때 `Add to PATH` 체크 안 하신 겁니다. 3단계 다시 |
| `ModuleNotFoundError` | 5단계를 안 하신 겁니다 |
| `start.bat` 눌렀는데 창이 깜빡하고 사라짐 | 5단계 안 된 것입니다 |
| 메일에서 `미리보기 —` 나옴 | 파일 이름이 `mail.env.txt` 일 확률 99% |
| 메일에서 `SMTPAuthenticationError` | 앱 비밀번호 말고 로그인 비밀번호를 넣으신 겁니다 |

안 되면 **검은 창 전체가 보이게 캡처**해서 보내주세요.
